import DashboardLayout from "../../components/student/layouts/DashboardLayout";
import { Fragment } from "react";

const ExpenseTracking = (props) => {
  return (
    <Fragment>
      <div>you are in expense tracking page</div>
    </Fragment>
  );
};

ExpenseTracking.layout = DashboardLayout;
export default ExpenseTracking;
